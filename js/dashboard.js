// Get the logout button element
const logoutButton = document.getElementById('logoutButton');

// Add event listener for logout button click
logoutButton.addEventListener('click', function() {
  // Perform logout logic (replace with your own implementation)
  // For example, you can clear session data, redirect to the login page, or perform any other desired actions
  alert('Logout successful!');
  window.location.href = 'website.html'; 
});